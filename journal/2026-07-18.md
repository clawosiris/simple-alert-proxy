# 2026-07-18

- `main` advanced from `b7b1b53` to `3cd0804` when PR `#39` merged, expanding input setup documentation for generic JSON webhooks, supported presets, and an OpenVAS example.
- PR `#40` opened to align the README with the gateway now shipped on `main`; its CI, CodeQL, SBOM, and devel image checks passed.
- Issue `#41` and PR `#42` opened to make SigNoz/Alertmanager intake a configured built-in integration, unify configured webhook routing, and retain legacy `server.webhook_path` compatibility. PR checks passed.
- The OpenSSF Scorecard push workflow failed on the new `devel` branch because Scorecard supports only the default branch; the corresponding `main` Scorecard run remained successful.
