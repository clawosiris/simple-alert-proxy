# 2026-07-31

- PR `#58` hardened GitHub Actions usage and merged to `main`.
- PR `#60` fixed replay handling so the delivery worker runs after replayed alerts are queued.
- PR `#59` redacted alert event payloads returned by the management API, closing issue `#56`.
- PR `#61` updated `ossf/scorecard-action` to `2.4.4`.
- CI, CodeQL, SBOM, and the July 30–31 nightly builds pass on the current `main`; OpenSSF Scorecard remains failing because result publication rejects the pinned `github/codeql-action/upload-sarif` commit as an “imposter commit.”
