# 2026-07-22

- PR `#46` merged configurable alert retention into `main`. `storage.retention_days` defaults to 90 days, rejects non-positive values, and drives startup plus daily pruning of expired alerts and dependent delivery, audit, advisory, escalation, group, and orphan records.
- Issue `#45` closed with the retention merge. CI, CodeQL, SBOM, and OpenSSF Scorecard passed on the resulting `main` commit, and its scheduled Nightly build also succeeded.
- PR `#47` opened to prepare release `v0.0.10`; its CI, CodeQL, and SBOM checks passed, but the PR remains open and blocked from merging.
- Issue `#30` was narrowed after the user/session foundation merged: remaining RBAC work is team ownership and server-side scoping for routes, receivers, alert groups, lifecycle actions, UI filters, and audit context, with separate scoped PRs recommended.
