# 2026-07-14

- No new default-branch commits, tags, or releases landed after the `2026-07-13` journal entry; `main` remains at `77d2a9324dae` and release `v0.0.8`.
- PR `#31` (`feat: add local user management`) advanced with commit `78809377acc9` to revoke existing management sessions after password changes, tightening the local user/RBAC slice before review.
- GitHub checks for PR `#31` were green on the new head across `CI` (`rust` and `container`), `CodeQL`, and `SBOM`; the PR remains blocked on required review.
- PR `#32` (`fix: clear stale lifecycle state when alerts reactivate`) remains open and review-blocked with no material movement since the prior journal entry.
