# 2026-07-24

- Issues `#56` and `#57` identified management API exposure of stored raw alert payloads and GitHub Actions hardening gaps.
- PR `#59` adds read-side redaction of sensitive fields in management API alert-event payloads while retaining the stored payload for forensics. It is approved, mergeable, and its CI, CodeQL, and SBOM checks passed.
- PR `#58` pins workflow actions to reviewed commit SHAs, reduces token persistence and job permissions, and keeps CI, CodeQL, and SBOM checks green; it remains open awaiting review.
