# 2026-09-14

- Merged PR `#95`, replacing the SigNoZ/Google Chat in-memory aggregator with generic durable notification batching. Batches and member deliveries are persisted in SQLite with startup recovery, retries, and dead-letter handling; batching now covers Google Chat, generic webhooks, Slack, Mattermost, Discord, and Matrix.
- Merged PR `#96`, adding bounded MiniJinja notification templates with strict undefined handling and startup validation. Standard title/body templates and guarded JSON payload templates are supported across all outbound receivers, while deterministic template failures dead-letter without making HTTP requests.
- Merged the `h2` security update for `RUSTSEC-2026-0258` and several dependency/tooling updates, including `argon2`, `rusqlite`, `uuid`, `tower-http`, CodeQL upload, and SBOM tooling.
- CI, CodeQL, SBOM, OpenSSF Scorecard, and the post-merge nightly run all passed on `main` at `15399d3`.
