# 2026-09-02

- Dependabot opened dependency updates `#87` (uuid 1.26.0), `#88` (argon2 0.6.0), `#89` (Anchore download-syft 0.24.2), and `#90` (CodeQL upload-sarif 4.37.9). CI, CodeQL, and SBOM checks pass for `#87`, `#89`, and `#90`.
- The Argon2 0.6 update in `#88` is not source-compatible with the current password-hashing code: CI fails on the removed `SaltString` import, deprecated `PasswordHash` alias, and changed `hash_password` signature. The Rust and container jobs require code changes before this major dependency update can merge.
