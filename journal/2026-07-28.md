# 2026-07-28

- PR `#60` fixes a delivery correctness bug where `/api/deliveries/{id}/replay` returned `202 Accepted` and marked the delivery queued without starting a worker to resend the alert.
- The fix reconstructs replay context from the persisted alert event and delivery summary, and adds a regression test verifying that replay sends a second payload to the receiver.
- PR `#60` is approved and its CI, CodeQL, and SBOM checks passed; it remains open.
