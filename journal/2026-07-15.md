# 2026-07-15

- `main` advanced from `77d2a9324dae` to `1091697` after PR `#31` (`feat: add local user management`) and PR `#32` (`fix: clear stale lifecycle state when alerts reactivate`) merged, moving both previously review-blocked operator-auth and alert-lifecycle fixes into the default branch.
- PR `#33` (`fix: avoid duplicate event persistence for multi-route alerts`) opened against `main` with green `CI`, `CodeQL`, and `SBOM` checks, continuing the alert-routing correctness work.
- PR `#34` (`chore: bump version to 0.0.9`) opened from `release/v0.0.9` with green `CI`, `CodeQL`, and `SBOM` checks; latest published release remains `v0.0.8` until that release-prep PR lands and is tagged.
- Dependabot opened PRs `#35` through `#38` for `sha2`, `uuid`, `regex`, and `rustls`; their initial `CI`, `CodeQL`, and `SBOM` checks completed successfully and they remain review-required.
