# 2026-08-02

- PR `#62` merged, adding team ownership to routes, receivers, and persisted alert groups; enforcing team-scoped read and lifecycle permissions; and completing the ownership/scoping work tracked by issue `#30`, which is now closed.
- PR `#63` merged, adding static on-call schedules whose responders target a receiver, user, or team, and allowing escalation policy steps to reference those schedules. CI, container, CodeQL, SBOM, and OpenSSF Scorecard checks pass on the resulting `main` commit.
- PR `#64` opened to execute escalation chains with a background worker, broader step targets, delivery queuing, and cancellation after acknowledgment or resolution. Its CI, container, CodeQL, and SBOM checks pass; it remains open for review.
