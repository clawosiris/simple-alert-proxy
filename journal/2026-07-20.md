# 2026-07-20

- PR `#40` merged `devel` into `main`, promoting configured built-in SigNoz/Alertmanager intake, unified configured webhook routing, and nightly container builds to the default branch.
- Issues `#41` and `#43` closed with the promotion. CI, CodeQL, SBOM, and OpenSSF Scorecard all passed on the resulting `main` commit.
- The first scheduled Nightly workflow on the promoted `main` revision completed successfully.
