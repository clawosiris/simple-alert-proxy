# 2026-09-08

- Security PR `#91` updates the locked `h2` dependency from 0.4.14 to 0.4.16 to resolve `RUSTSEC-2026-0258`. The lockfile-only fix passes Rust CI, container CI, CodeQL, and SBOM checks; it remains open and requires review before merge.
- The dedicated Hetzner GitHub Actions runner is active and handled the security PR checks successfully.
