# 2026-07-11

- No new commits or releases landed on `main` after `77d2a9324dae` / PR `#29`, so the default-branch production baseline remains release `v0.0.8`.
- The issue tracker materially changed direction with issue `#30` opening a new user-management and RBAC stream: named local users, teams, management sessions, audit attribution, and server-enforced roles while preserving the legacy management bearer token as a compatibility/bootstrap path.
- PR `#31` (`feat: add local user management`) opened against `main` and was materially updated with a hardening follow-up commit. The branch adds SQLite-backed users, teams, memberships, and sessions; Argon2id password hashing; bootstrap-admin env support; HttpOnly cookie login plus CSRF checks; `admin`/`operator`/`viewer` roles; basic WebUI user/team controls; and docs/config updates. Its GitHub `CI`, `CodeQL`, and `SBOM` checks were green after the latest update, but the PR still requires review before merge.
