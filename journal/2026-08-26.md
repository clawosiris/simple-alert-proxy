# 2026-08-26

- Dependabot closed superseded CodeQL action PR `#84` and opened PR `#85` to update `github/codeql-action/upload-sarif` from 4.37.4 to 4.37.8; CI, CodeQL, and SBOM checks passed on the replacement PR.
- The scheduled Cargo dependency update failed because Dependabot could not resolve the locked `blake2` 0.10.6 package from the crates.io index. Existing Cargo update PR `#83` remains open with passing checks, but automated Cargo refreshes now require attention.
