# 2026-08-12

- Merged PR `#72`, adding native Matrix room delivery with configuration validation, stable transaction IDs across retries, fresh IDs for manual replays, endpoint and room-ID validation, safe HTML links, tests, and examples.
- Merged PRs `#73` and `#75`, adding first-class Grafana webhook normalization and routing with per-instance canonical events, stable lifecycle fingerprints, mixed-group status handling, and isolated per-instance raw payloads.
- Merged PR `#69`, making escalation chains honor acknowledgement and resolution stop conditions while preserving SQLite migration compatibility.
- Opened issue `#76` after identifying that Grafana fingerprints and group keys are not namespaced by integration or organization, allowing independent tenants with matching source identifiers to mutate the same alert group; this remains an actionable isolation gap.
- Merged dependency updates for `rustls`, `clap`, and the OpenSSF Scorecard action. CI, CodeQL, SBOM, Scorecard, and the subsequent nightly run on `main` are green. Four new Dependabot PRs (`#77`-`#80`) are open with green checks.
