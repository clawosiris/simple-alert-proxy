# 2026-07-19

- PR `#42` merged into `devel`, adding configured built-in SigNoz/Alertmanager intake, unified configured webhook routing, and legacy `server.webhook_path` compatibility.
- Issue `#43` and PR `#44` added nightly Rust validation and container builds on `devel`; publishing from `main` is designed to produce `nightly`, dated, and short-SHA image tags.
- PR `#40` remains the promotion path from `devel` to `main`; its CI, CodeQL, SBOM, and container checks passed after both merges. The previously known OpenSSF Scorecard failure on non-default branches remains unchanged.
