# 2026-07-23

- PR `#47` merged the `v0.0.10` version bump into `main`, and release `v0.0.10` was published. CI, CodeQL, SBOM, OpenSSF Scorecard, and the release workflow passed on the release commit.
- Dependabot PRs `#48`–`#55` merged updates for `serde_json`, `regex`, `thiserror`, `uuid`, `tokio`, `clap`, `anyhow`, and `serde`; the resulting `main` checks passed.
- The scheduled Nightly workflow completed successfully against the `v0.0.10` `main` commit.
