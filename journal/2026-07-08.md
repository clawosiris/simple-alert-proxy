# 2026-07-08

- `main` absorbed PR `#12` (`feat: implement alert proxy v2 gateway`) at 2026-07-08T00:13:52Z, shifting the service from a SigNoz-specific forwarder toward a broader alert gateway with a canonical `AlertEvent` model, generic `POST /webhooks/{integration}` ingress, SQLite-backed event/group/delivery/audit storage, retry and dead-letter delivery handling, alert lifecycle APIs, an operator UI at `/` and `/ui`, multi-target outbound adapters, escalation scheduling, advisory scaffolding, updated docs/examples, and synthetic end-to-end coverage.
- The same merge train expanded repo automation around the new gateway by adding a `devel` image workflow and updating CI, CodeQL, and release workflow coverage alongside the v2 code path.
- Follow-up issue `#11` opened on 2026-07-07 to expand end-to-end alert proxy test coverage for the new gateway behavior.
- Dependabot PRs `#9`, `#5`, `#4`, `#2`, and `#3` all merged into `main` between 2026-07-08T00:33:45Z and 2026-07-08T00:33:56Z, updating `anyhow`, `rustls`, `actions/checkout`, `tower-http`, and `axum-server`.
- Dependabot PR `#13` opened on 2026-07-08 against `main` to bump `rusqlite` from `0.32.1` to `0.40.1`.
- No new releases were published after `v0.0.4`, and this check did not find a new actionable CI or code-scanning state change beyond the workflow updates shipped with PR `#12`.
