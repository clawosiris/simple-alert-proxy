# 2026-07-09

- `main` picked up `feat: add debug webhook and health checks` at `332a9d0`, adding a debug ingress path plus health-check support and updating the container/unit docs alongside those runtime-facing endpoints.
- Release `v0.0.5` was published from `main` at `52d63e5`, making the post-v2 gateway changes available as the newest tagged build.
- Follow-up `main` commits `f887627`, `55a08cc`, and `91b440f` materially changed the container deployment guidance by adding quick-start commands, fixing volume-permission handling, and aligning the documented SQLite path with the container layout.
- Security and operations follow-up work was opened as issues `#14` through `#18`, covering exposed management API auth, operator UI authentication, the `quinn-proto` `RUSTSEC-2026-0185` audit failure, missing rate/concurrency limits, and sensitive-field redaction for debug alert logging.
- Dependabot PR `#13` (`rusqlite` `0.32.1` -> `0.40.1`) remains open and actionable: CodeQL passes, but its `CI` run is failing on the PR branch, so the dependency bump is not yet merge-ready.
